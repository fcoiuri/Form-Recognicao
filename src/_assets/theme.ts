import { createTheme } from "@material-ui/core";

export const theme = createTheme({
  palette: {
    primary: {
      main: "#2E8B57",
    },
    secondary: {
      main: "#454B1B",
    },
  },
  typography: {
    fontFamily: "Poppins, sans-serif",
  },
});
